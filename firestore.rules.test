rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEST MODE - Allow all read/write for development
    // ⚠️ WARNING: Change to production rules before deploying!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

